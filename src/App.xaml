<Application x:Class="PoE2LevelingCompanion.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Minimal button style for window chrome -->
        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#999999" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33ffffff" />
                                <Setter Property="Foreground" Value="#ffffff" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Converter: count == 0 -> Visible, else Collapsed -->
        <local:EmptyToVisibilityConverter x:Key="EmptyToVisibility"
            xmlns:local="clr-namespace:PoE2LevelingCompanion.Converters" />
        <local:LevelDifferenceToBrushConverter x:Key="LevelDiffBrush"
            xmlns:local="clr-namespace:PoE2LevelingCompanion.Converters" />
        <local:ZoneLevelToVisibilityConverter x:Key="ZoneLevelVisibility"
            xmlns:local="clr-namespace:PoE2LevelingCompanion.Converters" />
        <local:SplitDeltaToBrushConverter x:Key="SplitDeltaBrush"
            xmlns:local="clr-namespace:PoE2LevelingCompanion.Converters" />
    </Application.Resources>
</Application>
