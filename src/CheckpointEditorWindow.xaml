<Window x:Class="PoE2LevelingCompanion.CheckpointEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PoE2LevelingCompanion.ViewModels"
        Title="Checkpoint Editor"
        Width="600" Height="500"
        MinWidth="480" MinHeight="350"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">

    <Window.DataContext>
        <vm:CheckpointEditorViewModel />
    </Window.DataContext>

    <Window.Resources>
        <!-- Dark scrollbar (order matters: dependencies first) -->
        <Style x:Key="DarkThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#555577" CornerRadius="4" Margin="2" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="VerticalScrollBar" TargetType="ScrollBar">
            <Grid Width="10">
                <Border Background="#1a1a2e" CornerRadius="4" />
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource DarkThumb}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#1a1a2e" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
                    <Setter Property="Width" Value="10" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e8a735" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#aaaaaa" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,6,0" />
        </Style>

        <Style x:Key="EditorTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2a2a3e" />
            <Setter Property="Foreground" Value="#e0e0e0" />
            <Setter Property="BorderBrush" Value="#444466" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="CaretBrush" Value="#e0e0e0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <!-- Dark ComboBox toggle button -->
        <ControlTemplate x:Key="DarkComboBoxToggle" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border Grid.ColumnSpan="2" Background="#2a2a3e" BorderBrush="#444466" BorderThickness="1" />
                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                      Fill="#aaaaaa" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </ControlTemplate>

        <!-- Dark ComboBox template -->
        <ControlTemplate x:Key="DarkComboBoxTemplate" TargetType="ComboBox">
            <Grid>
                <ToggleButton Template="{StaticResource DarkComboBoxToggle}"
                              Focusable="False"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                              ClickMode="Press" />
                <ContentPresenter IsHitTestVisible="False"
                                  Content="{TemplateBinding SelectionBoxItem}"
                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                  Margin="6,4,23,4"
                                  VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Popup Placement="Bottom"
                       IsOpen="{TemplateBinding IsDropDownOpen}"
                       AllowsTransparency="True" Focusable="False"
                       PopupAnimation="Slide">
                    <Grid SnapsToDevicePixels="True"
                          MinWidth="{TemplateBinding ActualWidth}"
                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                        <Border Background="#2a2a3e" BorderBrush="#444466" BorderThickness="1" />
                        <ScrollViewer Margin="1" SnapsToDevicePixels="True"
                                      CanContentScroll="False"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                        </ScrollViewer>
                    </Grid>
                </Popup>
            </Grid>
        </ControlTemplate>

        <Style x:Key="EditorComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2a2a3e" />
            <Setter Property="Foreground" Value="#e0e0e0" />
            <Setter Property="BorderBrush" Value="#444466" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template" Value="{StaticResource DarkComboBoxTemplate}" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#2a2a3e" />
                        <Setter Property="Foreground" Value="#e0e0e0" />
                        <Setter Property="Padding" Value="6,4" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#444466" />
                                            <Setter Property="Foreground" Value="#ffffff" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3a3a5e" />
                                            <Setter Property="Foreground" Value="#e8a735" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#e8a735" />
            <Setter Property="Foreground" Value="#1a1a2e" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f0b845" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallButton" TargetType="Button">
            <Setter Property="Background" Value="#44ffffff" />
            <Setter Property="Foreground" Value="#1a1a2e" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#66ffffff" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RemoveButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#666666" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#cc5555" />
                                <Setter Property="Background" Value="#22cc5555" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <CollectionViewSource x:Key="GroupedZones"
                              Source="{x:Static vm:CheckpointEditorViewModel.ZoneOptionsStatic}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Act" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Log file path -->
        <Border Grid.Row="0" Background="#2a2a3e" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="PoE2 client.txt log file" Style="{StaticResource SectionHeader}" />
                <Button DockPanel.Dock="Right" Content="Browse..."
                        Click="BrowseLogFile_Click"
                        Style="{StaticResource SmallButton}"
                        Foreground="#e8a735" Margin="8,0,0,0" />
                <TextBox x:Name="LogFilePathBox" IsReadOnly="True"
                         Style="{StaticResource EditorTextBox}"
                         Text="(not set — will auto-detect)" />
            </DockPanel>
        </Border>

        <!-- Add checkpoint form -->
        <Border Grid.Row="1" Background="#2a2a3e" CornerRadius="6" Padding="12" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Add Checkpoint" Style="{StaticResource SectionHeader}" />

                <!-- Type + Value + Class -->
                <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="120" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Type:" Style="{StaticResource FieldLabel}" />
                    <ComboBox Grid.Column="1" Style="{StaticResource EditorComboBox}"
                              ItemsSource="{x:Static vm:CheckpointEditorViewModel.TriggerOptions}"
                              SelectedItem="{Binding NewTrigger}" />

                    <TextBlock Grid.Column="3">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
                                <Setter Property="Text" Value="Zone:" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsZoneTrigger}" Value="False">
                                        <Setter Property="Text" Value="Level:" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Zone dropdown (visible when Zone trigger) -->
                    <ComboBox Grid.Column="4"
                              ItemsSource="{Binding Source={StaticResource GroupedZones}}"
                              DisplayMemberPath="ZoneName"
                              SelectedItem="{Binding NewZone}"
                              IsTextSearchEnabled="True"
                              TextSearch.TextPath="ZoneName"
                              MaxDropDownHeight="350">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox" BasedOn="{StaticResource EditorComboBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsZoneTrigger}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Style>
                        <ComboBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                   Foreground="#e8a735" Background="#1a1a2e"
                                                   Padding="6,4,6,2" FontSize="11" />
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ComboBox.GroupStyle>
                    </ComboBox>

                    <!-- Level text box (visible when Level trigger) -->
                    <TextBox Grid.Column="4"
                             Text="{Binding NewLevelText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource EditorTextBox}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsZoneTrigger}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <TextBlock Grid.Column="6" Text="Class:" Style="{StaticResource FieldLabel}" />
                    <ComboBox Grid.Column="7" Style="{StaticResource EditorComboBox}"
                              ItemsSource="{x:Static vm:CheckpointEditorViewModel.ClassOptionsStatic}"
                              SelectedItem="{Binding NewClass}" />
                </Grid>

                <!-- Message + Add -->
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="+ Add"
                            Command="{Binding AddCommand}"
                            Style="{StaticResource SmallButton}"
                            Foreground="#e8a735" Margin="8,0,0,0" />
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Message:" Style="{StaticResource FieldLabel}" />
                        <TextBox Style="{StaticResource EditorTextBox}"
                                 Text="{Binding NewMessage, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                </DockPanel>
            </StackPanel>
        </Border>

        <!-- Entries list -->
        <Grid Grid.Row="2">
            <ListBox ItemsSource="{Binding Entries}"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,4" Padding="10,8" Background="#2a2a3e" CornerRadius="4">
                            <DockPanel>
                                <Button DockPanel.Dock="Right" Content="&#x2715;"
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource RemoveButton}"
                                        Margin="8,0,0,0" />
                                <StackPanel>
                                    <DockPanel Margin="0,0,0,3">
                                        <Border Background="#33e8a735" CornerRadius="3" Padding="6,1" Margin="0,0,6,0"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding TriggerBadge}" Foreground="#e8a735" FontSize="10" />
                                        </Border>
                                        <TextBlock Text="{Binding TriggerLabel}" Foreground="#cccccc"
                                                   FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding ClassDisplay}"
                                                   Foreground="#666666" FontSize="10"
                                                   HorizontalAlignment="Right" VerticalAlignment="Center" />
                                    </DockPanel>
                                    <TextBlock Text="{Binding Message}" Foreground="#aaaaaa"
                                               FontSize="12" TextWrapping="Wrap" />
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Empty state -->
            <TextBlock Text="No checkpoints — add one above"
                       Foreground="#555555" FontSize="12"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Visibility="{Binding Entries.Count, Converter={StaticResource EmptyToVisibility}}" />
        </Grid>

        <!-- Bottom bar -->
        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <Button DockPanel.Dock="Right" Content="Save Checkpoints"
                    Command="{Binding SaveCommand}" Style="{StaticResource ActionButton}" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="#88cc88" FontSize="12"
                       VerticalAlignment="Center" Margin="0,0,12,0" />
        </DockPanel>
    </Grid>
</Window>
